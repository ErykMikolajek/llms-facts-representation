# Mapowanie domen i pruning

Przygotowanie rozłącznych danych development/holdout, eksploracyjne mapowanie
topograficzne, właściwe mapowanie aktywacji neuronów MLP, walidacja
selektywności SAE oraz budowa masek ekspertów.

```bash
python3 -m domain_mapping.domain_mlp_activation_mapping --help
python3 -m domain_mapping.domain_mlp_pruning --help
```
